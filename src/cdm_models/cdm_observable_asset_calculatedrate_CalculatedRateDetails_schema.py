# generated by datamodel-codegen:
#   filename:  cdm-observable-asset-calculatedrate-CalculatedRateDetails.schema.json
#   timestamp: 2026-05-07T14:39:41+00:00

from pydantic import BaseModel, Field
from . import cdm_observable_asset_calculatedrate_CalculatedRateObservations_schema


class CalculatedRateDetails(BaseModel):
    observations: cdm_observable_asset_calculatedrate_CalculatedRateObservations_schema.CalculatedRateObservations | None = Field(
        None, description="The observation dates and weights for each observation date."
    )
    weighted_rates: list[float] | None = Field(
        None, alias="weightedRates", description="The weighted value of each observation.", min_length=0
    )
    growth_factor: list[float] | None = Field(
        None,
        alias="growthFactor",
        description="The daily growth factors, showing the weighted rates divided by the day count basis plus one, giving how much the value grows for each step in the calculation.",
        min_length=0,
    )
    compounded_growth: list[float] | None = Field(
        None,
        alias="compoundedGrowth",
        description="The compounding curve, showing how the initial value grew during the calculation period.",
        min_length=0,
    )
    aggregate_value: float | None = Field(
        None, alias="aggregateValue", description="The total sum or product of all the individual terms that went into the calculated rate."
    )
    aggregate_weight: float | None = Field(
        None, alias="aggregateWeight", description="The total weight of all the terms that went into the calculated rate."
    )
    calculated_rate: float | None = Field(None, alias="calculatedRate", description="The resulting calculated weight.")
